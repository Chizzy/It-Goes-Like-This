import React, { Component } from 'react';
import axios from 'axios';
import styled from 'styled-components'

class LogIn extends Component {
    render() {
        return (
            <div>
                <h1>Hello from Log In Page</h1>
            </div>
        );
    }
}

export default LogIn;